# Obsikit Template Upstream

This repo vendors a pinned scaffold snapshot of `obsikit` for `obsigen` publishing.

- Upstream repository: `git@github.com:gammaglitch/obsikit.git`
- Pinned commit: `1965227aa414ae6b0dac6980bb18823d198fac6f`

The vendored template lives under `templates/obsikit/`.

Rules:

- do not treat the local `obsikit/` clone as part of the main `obsigen` repo
- update the vendored template deliberately from the upstream `obsikit` repo
- keep this file in sync when the pinned template revision changes
